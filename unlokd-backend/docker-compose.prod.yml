# Production override. Use with:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# Set JWT_SECRET and MYSQL_ROOT_PASSWORD via environment or .env when running in production.

services:
  backend:
    environment:
      NODE_ENV: production
      CORS_ORIGIN: http://ec2-18-188-12-41.us-east-2.compute.amazonaws.com:5173

  frontend:
    build:
      context: ../unlokd-frontend
      args:
        VITE_API_BASE_URL: http://ec2-18-188-12-41.us-east-2.compute.amazonaws.com:3000
        VITE_WS_BASE_URL: http://ec2-18-188-12-41.us-east-2.compute.amazonaws.com:3000
